# src/

This folder contains the Python source code for the project.

## Main script
- `decision_tree_insurance.py` — trains a decision tree on the training dataset and scores new applicants.

## How to run
From the repository root:

```bash
python src/decision_tree_insurance.py
